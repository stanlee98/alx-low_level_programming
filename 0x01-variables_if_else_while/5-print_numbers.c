#include <stdio.h>

/**
* main - prints is posetive, negative
* or zero
*Return: returns zero if program exits well.
 */
int main(void)
{
int c = 0;
while (c <= 9)
{
printf("%i", c);
c++;
}
printf("\n");
return (0);
}
