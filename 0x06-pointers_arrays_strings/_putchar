/**
 * _putchar - writes the character c to stdout
 * @c: The character that'll be printed
 *
 * Return: 1 would be returned when succesfull
 * On error, -1 is returned, and  error is set
 */
int _putchar(char c)
{
return (write(1, &c, 1));
}
